    <div class='container'>
        <div class='row'>
            <% @books.each do |book| %> 
                <%= render partial: "books/cardbook", locals: { book: book } %>
            <% end %> 
        </div>
    </div>  