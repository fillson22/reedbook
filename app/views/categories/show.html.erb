    <%= turbo_frame_tag "first_turbo_frame" do %>
            <div class='row'>
                <% @books.each do |book| %> 
                    <%= render partial: "books/cardbook", locals: { book: book } %>
                <% end %> 
            </div>
    <% end %>