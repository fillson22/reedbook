<nav class="navbar navbar-expand-lg bg-info">
  <div class="container-fluid">
    <a class="navbar-brand"> <%= link_to "BUKR", root_path, class: "btn" %> </a>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item"> <%= link_to "Home", root_path, class: "btn btn-outline-light", style: 'color:#000000; margin-right:5px;' %> </li>
        <li class="nav-item"> <%= link_to "Books", books_path, class: "btn btn-outline-light", style: 'color:#000000; margin-right:5px;' %> </li>
      </ul>
      <ul class='nav'>
        <% if user_signed_in? %>
          <li class="nav-item dropdown">
            <%= link_to current_user.view_email, '#', class: "nav-link dropdown-toggle", data: { 'bs-toggle': 'dropdown'} %>
            <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
              <li> <%= link_to "Favorite book", '#', class: "dropdown-item" %> </li>
              <li><hr class="dropdown-divider"></li>
              <li> <%= link_to "Log out", destroy_user_session_path, method: :delete, class: "dropdown-item" %> </li>
            </ul>
          </li>  
        <% else %>
            <%= link_to "Sign in", new_user_session_path, class: "btn btn-warning", style: 'color:#000000; margin-right:5px;' %>
        <% end %>
      </ul>
      <form class="d-flex" role="search">
        <input class="form-control me-2" type="search" placeholder="Search" aria-label="Search">
        <button class="btn btn-outline-light" type="submit">Search</button>
      </form>
    </div>
  </div>
</nav>
